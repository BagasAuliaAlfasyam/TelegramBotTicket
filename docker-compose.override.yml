# ============================================================
# Production Override — uses pre-built images from
# Artifact Registry instead of local builds.
#
# Usage:
#   export IMAGE_REGISTRY=asia-southeast2-docker.pkg.dev/mytech-480618/microservices/
#   export IMAGE_TAG=abc1234
#   docker compose -f docker-compose.yml \
#                  -f docker-compose.override.yml \
#                  --env-file .env.local up -d --no-build
# ============================================================

services:
  prediction-api:
    image: ${IMAGE_REGISTRY}prediction-api:${IMAGE_TAG:-latest}

  data-api:
    image: ${IMAGE_REGISTRY}data-api:${IMAGE_TAG:-latest}

  collector-bot:
    image: ${IMAGE_REGISTRY}collector-bot:${IMAGE_TAG:-latest}

  admin-bot:
    image: ${IMAGE_REGISTRY}admin-bot:${IMAGE_TAG:-latest}

  training-api:
    image: ${IMAGE_REGISTRY}training-api:${IMAGE_TAG:-latest}
